{
  "scripts": {
    "start": "npm-run-all --parallel start-frontend start-backend",
    "start-frontend": "cd client && npm run dev",
    "start-backend": "cd server && npm run serverstart"
  },
    "dependencies": {
        "npm-run-all": "^4.1.5",
        "openai": "^4.38.5",
        "react-bootstrap": "^2.10.2"
    }
}
